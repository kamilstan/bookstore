export interface CustomerEntity {
    id?: string;
    userId?: string;
    fullName: string;
    email: string;
}

export interface CustomerFrontEntity {
    id: string;
    userId: string;
    fullName: string;
    email: string;
}